join(
				doc("book.xml")//Book,
				doc("books.xml")//book,
				[Year],
				[year]
			)
			

###
for $a in doc("book.xml")//Book,
	$b in doc("books.xml")//book
where $a/Year/text() = $b/year/text()
return <tuple>{
			$a/*,
			$b/*
			}</tuple>